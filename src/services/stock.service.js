/**
 * Stock Service
 * Business logic for user-facing stock operations
 * Note: This service is now minimal as we use marketWindowStockService for all operations
 */
class StockService {
  // This service is kept for backward compatibility but all operations
  // are now handled by marketWindowStockService
}

module.exports = new StockService();

